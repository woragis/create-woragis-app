import { useFooterModel } from './model'

export const FooterView = ({}: ReturnType<typeof useFooterModel>) => {
  return (
    <footer>
      <center>
        <h1>Footer</h1>
      </center>
    </footer>
  )
}
